import {Command} from '@oclif/command'

export default class ListPlugins extends Command {
  static description = 'List all available plugins to use with daSWAG CLI.'

  async run() {
    this.log('Coming soon...')
  }
}
